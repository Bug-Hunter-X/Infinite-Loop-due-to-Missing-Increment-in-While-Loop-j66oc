public class ExampleClass
{
    public int MyProperty { get; set; }

    public void MyMethod()
    {
        int i = 0;
        while (i < 10)
        {
            //Increment the variable i to terminate the loop.
            i++;
        }
    }
}